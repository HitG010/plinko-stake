import { useParams } from "react-router-dom";

export default function Dashboard() {
    const { user } = useParams();
    
    const 
  return (
    <div>
      <h1>Dashboard</h1>
      <h3>Hi, {}</h3>
    </div>
  );
}
